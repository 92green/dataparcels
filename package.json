{
  "private": true,
  "scripts": {
    "prepublishOnly": "yarn build",
    "build": "cp README.md packages/parcels/README.md && cp README.md packages/parcels-react/README.md && lerna run build --parallel --",
    "flow": "blueflag-test flow",
    "lint": "blueflag-test lint --monorepo",
    "test": "blueflag-test test --monorepo",
    "coverage": "blueflag-test coverage --min-coverage=60 --monorepo",
    "test-all": "yarn lint && yarn coverage",
    "deploy-docs": "yarn lerna --scope parcels-docs run deploy",
    "view-coverage": "yarn run coverage; open ./coverage/lcov-report/index.html",
    "watch": "lerna run watch --parallel --"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/gatsby*",
      "**/gatsby*/**"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.0",
    "babel-preset-blueflag": "^0.6.0",
    "blueflag-test": "^0.18.1",
    "enzyme": "^3.3.0",
    "lerna": "^2.9.0",
    "proto-babel-plugin-extract-flow-types": "git+ssh://git@github.com/blueflag/proto-babel-plugin-extract-flow-types.git#v0.1.1"
  }
}
