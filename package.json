{
  "private": true,
  "scripts": {
    "prep": "yarn && yarn lerna run yalc-publish && yarn lerna run yalc-update && yarn lerna bootstrap",
    "bootstrap": "yarn prep",
    "build": "yarn lerna run build",
    "build-docs": "yarn copy-readmes && cd packages/dataparcels-docs && yarn build-docs",
    "bump": "yarn lerna publish",
    "copy-readmes": "cp README.md packages/dataparcels/README.md && cp README.md packages/react-dataparcels/README.md",
    "preversion": "yarn build",
    "reinstall": "rm -rf node_modules && rm -rf packages/*/node_modules && rm -rf packages/*/.yalc && yarn prep",
    "test-all": "yarn lerna run test-all",
    "watch": "yarn lerna run watch"
  },
  "dependencies": {
    "lerna": "^3.10.7"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.2.1",
    "husky": "^1.1.2",
    "yalc": "^1.0.0-pre.27"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
